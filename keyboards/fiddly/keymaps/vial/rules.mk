VIA_ENABLE = yes
VIAL_ENABLE = yes
VIAL_INSECURE = yes

# Report the press immediately and ignore the pin for the window, instead of
# waiting the window out after every transition as sym_defer_pk does.
DEBOUNCE_TYPE = sym_eager_pk
